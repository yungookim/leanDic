"use strict";
window.ExampleModel = Backbone.Model.extend({
  defaults: {
  },

  initialize : function(){

  },

  fetch : function(id, next){

  }
});

window.ExampleCollection = Backbone.Collection.extend({
 
});